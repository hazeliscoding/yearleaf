{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main-window permissions: core defaults plus the typed desk, event and attachment commands. Nothing else crosses IPC (docs/design-sqlite-persistence.md).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-load-desk",
    "allow-save-object",
    "allow-delete-object",
    "allow-load-events",
    "allow-save-event",
    "allow-delete-event",
    "allow-import-attachment",
    "allow-load-attachments"
  ]
}
