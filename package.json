{
  "name": "yearleaf",
  "version": "0.1.0",
  "private": true,
  "description": "Infinite Desk Calendar — a local-first spatial calendar where time is an infinite canvas.",
  "packageManager": "pnpm@12.4.2",
  "scripts": {
    "dev": "pnpm --filter desktop start",
    "build": "pnpm --filter desktop build",
    "test": "pnpm run test:packages && pnpm --filter desktop test && pnpm run test:rust",
    "test:all": "pnpm run test && pnpm run test:e2e",
    "test:packages": "vitest run --config vitest.packages.config.ts",
    "test:rust": "cargo test --manifest-path apps/desktop/src-tauri/Cargo.toml",
    "test:e2e": "pnpm --filter desktop e2e"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vitest": "^4.0.8"
  }
}
