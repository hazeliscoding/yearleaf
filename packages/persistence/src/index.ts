/**
 * `@infinite-desk/persistence` — storage contracts and the in-memory
 * adapter. Concrete SQLite/IndexedDB adapters implement these interfaces
 * behind the same seam.
 */

export * from './desk-persistence';
